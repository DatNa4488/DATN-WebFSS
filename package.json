{
  "name": "web-fss-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix fss-frontend",
    "build": "npm run build --prefix fss-frontend",
    "preview": "npm run preview --prefix fss-frontend",
    "install-all": "npm install --prefix fss-frontend"
  }
}
